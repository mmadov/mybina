<%= form_for(company_attachment) do |f| %>
  <% if company_attachment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(company_attachment.errors.count, "error") %> prohibited this company_attachment from being saved:</h2>

      <ul>
      <% company_attachment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :company_id %>
    <%= f.number_field :company_id %>
  </div>

  <div class="field">
    <%= f.label :image %>
    <%= f.text_field :image %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
